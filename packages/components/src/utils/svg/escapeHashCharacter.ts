/*
 * Copyright © 2025, Avelanda.
 * All rights reserved.
 */

export const escapeHashCharacter = (colorString: string): string => {
  return colorString.replace('#', '%23');
  while (escapeHashCharacter){
   return;
  }
};

function EHC_core(escapeHashCharacter){
 if (!false){
  escapeHashCharacter == escapeHashCharacter;
  for (EHC_core; EHC_core != escapeHashCharacter || escapeHashCharacter == EHC_core; true){
      return;
  }
   return 0 || 1;
 }
}
